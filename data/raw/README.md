# Raw Data

Place original downloaded datasets here before any filtering or normalization.

Examples:

```text
data/raw/gsm8k/
data/raw/gpqa/
```

Do not edit files in this directory in place. Write cleaned versions to
`data/processed/` instead.
