# Outputs

Generated experiment artifacts belong here and are ignored by git by default.

Recommended layout:

```text
outputs/runs/      Main JSONL run outputs from hitl-run
outputs/figures/   Regenerated plots or tables, if needed
outputs/logs/      Long local logs, if not written elsewhere
```

Commit only lightweight documentation or intentionally selected final artifacts.
