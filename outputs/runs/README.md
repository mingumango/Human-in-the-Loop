# Run Outputs

Write experiment JSONL outputs here.

Example:

```bash
hitl-run \
  --config configs/gsm8k_no_reflection_llama8b.example.yaml \
  --output outputs/runs/gsm8k_no_reflection_llama8b.jsonl
```

Run outputs can be resumed with `--resume` when the output file already exists.
